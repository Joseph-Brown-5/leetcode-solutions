# These are some of the provided methods used to manipulate string variables
# type: print(help(str)) for a full list of provided methods

name = input("Enter your full name: ")
phone_number = "123-456-7890"

# result = len(name)
# result = name.find(" ") #.find() finds the first occurance of the given string value
# result = name.rfind("o") #.rfind() finds the last occurance of the given string value (will return -1 if no results)
# name = name.capitalize() #.capitalize() will capitalize the first letter of the string
# name = name.upper() #.upper() will uppercase all the values in a string
# name = name.lower() #.lower() will lowercase all values in a string
# result = name.isdigit() #.isdigit() will return a boolean based on whether the string contains only numbers or not
# result = name.isalpha() #.isalpha() will return a boolean based on whether the string contains only letters (spaces dont count)
# result = phone_number.count("-") #.count() will return the amount of times the given string appears
# phone_number = phone_number.replace("-", " ") #.replace() will replace the first given string with the second given string


print(name)